rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The website uses authenticated server API routes and Firebase Admin SDK.
    // No browser may read or write Trevo business data directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
